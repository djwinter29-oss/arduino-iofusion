[pytest]
minversion = 8.0
addopts = -ra --strict-config --strict-markers
testpaths = python_tests
python_files = test_*.py
norecursedirs = .git .venv .pio coverage docs/doxygen
markers =
	smoke: fast smoke tests for repository Python tooling
